# This is a program for Armstrong number.
# Take input from user.
num = int(input("Enter the number you want to check:"))
#Calculate the number of digits.
n = len(str(num))
# Initialise sum
sum = 0
# Copy num to perform the operations.
temp = num #temp is a temporary number.
# Calculate sum of the digits raised to the power of n.
while temp > 0:
    digit = temp % 10
    sum += digit ** n
    temp = temp // 10
# Check, the number is an Armstrong number or not.
if sum == num:
    print(f"{num} is Armstrong number.")
else:
    print(f"{num} is not Armstrong number.")